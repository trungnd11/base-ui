import { Color } from "@components/variable";
import styled from "styled-components";

export const AppButtonLinkStyle = styled.div`
  cursor: pointer;
  color: ${Color.blueText};
  text-decoration: underline;
`;
